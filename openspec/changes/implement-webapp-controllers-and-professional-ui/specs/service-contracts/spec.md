## MODIFIED Requirements

### Requirement: Web layer depends on BLL contracts, not repositories
The WebApp MUST consume BLL service interfaces rather than repository interfaces for business operations, including scaffolded MVC controllers that are later hardened for tenant-safe production use.

#### Scenario: Controller uses BLL contract
- **WHEN** a controller or page requires business operations
- **THEN** it depends on a BLL service interface and not a repository interface

#### Scenario: Scaffolded controller is hardened to BLL contract usage
- **WHEN** a controller is generated by scaffolding and initially contains direct data-access patterns
- **THEN** the finalized controller implementation is updated to call BLL service contracts for all business operations before workflow completion is accepted

